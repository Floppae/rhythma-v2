import React from "react";

const CreatorEditPage = () => {
  return (
    <div>
      select al maps where user_id === currentuid and display accordingly
    </div>
  );
};

export default CreatorEditPage;
